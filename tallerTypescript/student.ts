export class Student {
    codigo: number;
    cedula: number;
    edad: number;
    direccion: string;
    telefono: number;
    nombre: string;
  
    constructor(codigo: number, cedula: number, edad: number, direccion: string, telefono: number, nombre: string) {
      this.codigo = name;
      this.cedula = cedula;
      this.edad = edad;
      this.direccion = direccion;
      this.telefono = telefono;
      this.nombre = nombre;
    }
  }